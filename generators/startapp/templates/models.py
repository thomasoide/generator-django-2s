"""
This is the place where you should store your models. If your models have a large amount of tables, you should probably
divide them into multiple separate files in a directory. Make sure that the directory is located within this folder
and also has an __init__.py file to initialize the directory as a package.
"""

from django.db import models
from postgres_copy import CopyManager

# Create models below 
